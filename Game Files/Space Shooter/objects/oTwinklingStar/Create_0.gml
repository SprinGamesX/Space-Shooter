/// @description
image_blend = oMenuManager.c_rainbow;
alarm[0] = random(seconds(30));
twinkle = false;

image_alpha = random_range(0.5, 0.8);

image_speed = 0;

base_x = x;
base_y = y;

radius = 100;

time = 0;

mov_spd = random_range(0.001, 0.004);

spd = random_range(0.5, 1);